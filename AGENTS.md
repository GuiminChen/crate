# Vault agent hints

- 编译写入仅限 `wiki/**`；不要覆盖 `raw/**` 中的源文件。
- 遵守 `VAULT.md` 与项目 `docs/technical-design.md` 中的 front-matter 约定。
- 聊天与嵌入 API 的配置（多平台、`CRATE_LLM_PROVIDER`）见 **`docs/providers.md`**；CLI 与目录约定见 **`docs/usage.md`**。
